{
  "name": "agent-lens",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source observability for AI coding agents",
  "workspaces": [
    "core",
    "cli",
    "dashboard"
  ],
  "scripts": {
    "build": "npm run build -w core && npm run build -w dashboard",
    "dev": "npm run dev -w dashboard",
    "cli": "npm run dev -w cli",
    "test": "npm test -w core"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.2.0",
    "autoprefixer": "^10.5.4",
    "postcss": "^8.5.26",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
